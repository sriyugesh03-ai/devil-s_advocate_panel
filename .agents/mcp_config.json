{
  "mcpServers": {
    "tavily-search": {
      "command": "python",
      "args": ["-m", "backend.app.mcp.tools"],
      "env": {
        "TAVILY_API_KEY": "${TAVILY_API_KEY}"
      },
      "description": "Live Web & Competitor Search MCP for real-time market pricing and stealth competitor discovery."
    },
    "github-diligence": {
      "command": "python",
      "args": ["-m", "backend.app.mcp.tools"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
      },
      "description": "Technical Codebase Diligence MCP for repository commit velocity and architecture auditing."
    },
    "deck-parser": {
      "command": "python",
      "args": ["-m", "backend.app.mcp.tools"],
      "description": "Pitch Deck & PDF document ingestion parser MCP."
    }
  }
}
